# 囤囤馒头小程序 · 设计素材清单

扫码体验完小程序之后，对照这个清单出图就行。

画布用 750 宽（小程序的基准宽度），高度按实际内容定。所有尺寸标的是 rpx，750 宽画布里 1rpx = 1px，直接按数字做就好。


## 一、商品图

商品会在商家管理后台随时增减，所以不需要按具体品类出图。只需要出一张 **商品图模板 / 示例图**，告诉我这类图片的风格方向就行。我后续会照着这个规范让商家自己上传。

要求：**200×200px、PNG 或 JPG、白底或浅色背景、面包居中、不用加圆角（代码做了）**。

方便的话出 3~5 张不同品类的样图（比如一个吐司类、一个可颂类、一个饮料类），方便我做 UI 排版验证。文件名无所谓。


## 二、首页 Banner（1 张）

<img src="https://picsum.photos/750/560" width="375" />

第一屏顶部大图，尺寸 **750 × 560 rpx**。文件名 `banner_main.jpg`。品牌主视觉 + 一句 slogan，暖色调，面包店该有的氛围。


## 三、首页活动卡片（4 张）

Banner 下面的横向滚动区，尺寸 **260 × 220 rpx**，文件名 `activity_01.jpg` ~ `activity_04.jpg`。可以放"新店开业""会员日""新品上线"之类的活动图，四张风格统一。


## 四、底部导航图标（TabBar）

四个入口，每个出两张（默认态 + 选中态），**40 × 40 px，PNG 透明底**。

| 入口 | 默认态 | 选中态 |
|------|--------|--------|
| 首页 | `tab_home.png` | `tab_home_active.png` |
| 点单 | `tab_menu.png` | `tab_menu_active.png` |
| 订单 | `tab_orders.png` | `tab_orders_active.png` |
| 我的 | `tab_mine.png` | `tab_mine_active.png` |

> 默认态灰色（#999），选中态品牌金色（#f5a623），线性风格。


## 五、页面图标（5 个）

首页几个功能入口目前用的 emoji 占位，需要替成正式图标。**48 × 48 px，PNG 透明底**。

| 位置 | 命名 |
|------|------|
| 门店自提 | `icon_pickup.png` |
| 邮寄服务 | `icon_delivery.png` |
| 积分商城 | `icon_points.png` |
| 充值中心 | `icon_recharge.png` |
| 会员码 | `icon_member.png` |


## 六、品牌色

```
主色（按钮/选中）：#f5a623
背景色：          #FFEB99
页面底色：        #fdf6e3
```


## 七、交付方式

全部打包一个 zip 发我就行，文件名按上面的来。


*2025-06-08*
