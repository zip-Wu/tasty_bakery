<view class="container">
  <!-- 地图区域 -->
  <view class="map-wrap">
    <map
      id="storeMap"
      class="map"
      longitude="{{stores[0].longitude}}"
      latitude="{{stores[0].latitude}}"
      scale="15"
      markers="{{markers}}"
    ></map>
  </view>

  <!-- 底部选店区域 -->
  <view class="bottom-sheet">
    <view class="sheet-title">门店地址</view>

    <view class="store-list">
      <view wx:for="{{stores}}" wx:key="id" class="store-item {{selectedStoreId === item.id ? 'active' : ''}}" bindtap="selectStore" data-id="{{item.id}}">
        <view class="store-left">
          <view class="store-name">{{item.name}}</view>
          <view class="store-addr">{{item.address}}</view>
          <view class="store-hours {{item.open ? 'open' : 'closed'}}">营业时间：{{item.hours}}</view>
        </view>
        <view class="store-right">
          <view class="store-dist">{{item.distance}}</view>
          <view class="store-status {{item.open ? 'open' : 'closed'}}">{{item.open ? '接受预定中' : '暂无法预定'}}</view>
          <view class="nav-btn" catchtap="openNavigation" data-id="{{item.id}}">导航</view>
        </view>
      </view>
    </view>

    <view class="confirm-btn" bindtap="confirmStore">进入点单</view>
  </view>
</view>
